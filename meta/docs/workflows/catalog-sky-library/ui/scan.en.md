---
short_description: Counts the .sky files to index.
long_description: Entry point - counts the .sky files in the target directory and stops the run cleanly when there is nothing to index.
---

Pure bash. Counts `*.sky` in `{{dir}}`.
