---
short_description: Appends the bullet to CHANGELOG.md under a dated heading.
long_description: Prints the changelog bullet from the describe step and appends it to CHANGELOG.md in the target directory, under a heading with today's date. Creates the file when it does not exist yet.
---

Pure bash. The only step that writes anything.
