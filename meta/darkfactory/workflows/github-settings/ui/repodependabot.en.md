---
short_description: Enables Dependabot vulnerability alerts and automated security updates on the repo.
long_description: Turns on Dependabot vulnerability alerts and automated security fixes for the target repository. Free on every plan and visibility, so it is applied unconditionally whenever repo scope is in play.
---
Runs only when preflight computed `do_repo == 'true'`. Idempotent — enabling an already-enabled feature is a no-op.
