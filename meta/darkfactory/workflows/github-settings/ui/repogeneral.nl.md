---
short_description: "Past repo-merge-hygiëne toe: squash-only, auto-merge, delete-branch-on-merge."
long_description: "Configureert de merge- en feature-instellingen van de repository: squash-only merges, auto-merge, delete-branch-on-merge, allow-update-branch, en schakelt issues, wiki en projects in. Free-tier, elke zichtbaarheid — de universele basislijn die elke repo krijgt."
---
Draait alleen wanneer preflight `do_repo == 'true'` berekende. Elke call is een settings-upsert, dus herhaalde runs convergeren naar dezelfde staat.
