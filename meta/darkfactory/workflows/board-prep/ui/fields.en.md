---
short_description: Verifies the project carries the dark-factory field schema (read-only).
long_description: Deterministic bash + `gh`, no LLM, and strictly advisory. Checks whether the linked project has the custom fields from the Skylence project template (Status, Priority, Size, Idea Stage, Bug Stage) and all Status column options from Icebox through Done. It never edits the project.
---
Reports any missing field or Status option so you know whether the project template was applied. Creating or altering fields stays the template's job.
