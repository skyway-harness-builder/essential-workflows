---
short_description: Synthesizes a v1 eval suite of 3-5 checkable cases from the skill's promises.
long_description: Claude (haiku). Reads the skill source block and generates 3-5 concrete success cases that test what the skill promises to do, biased toward its stated triggers and MUST/MUST-NOT rules. Each case names the exact observable behavior that counts as a pass.
---
These are judge-generated evals; hand-authored fixtures are a deliberate follow-up. The numbered output is consumed verbatim by the exercise and judge steps.
