---
short_description: Opus controleert of de kandidaat werkelijk overeenkomt met de intentie van de operator.
long_description: Claude (opus). Leest het kandidaatbestand en beoordeelt trigger, node-vorm, gedrag op papier en volledigheid tegen `{{intent}}` — en markeert alles wat schoon lint maar intentie-fout is, zoals een `when` die nooit vuurt. Bij twijfel zegt het nee.
---
Het gestructureerde `output_format` is adviserend; het contract is de afsluitende machine-sentinelregel `SKY_FORGE delivers=<true|false>`, die de deterministische gate grept. Het oordeel is uitsluitend op papier — de workflow is niet uitgevoerd.
