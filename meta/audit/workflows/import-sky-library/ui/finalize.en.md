---
short_description: Copies the approved files into the target directory.
long_description: After the gate is approved, copies each staged PASS file into the target directory, never overwriting an existing file. Prints a kept/skipped manifest so you can see exactly what landed.
---
To replace an existing workflow, remove or rename the target file first and re-run — this step will not overwrite.
