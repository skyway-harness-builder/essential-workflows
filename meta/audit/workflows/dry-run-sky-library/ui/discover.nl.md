---
short_description: Globt de doelmap en produceert één naam/pad-regel per .sky-bestand.
long_description: Somt elk .sky-bestand op onder de map die via --var dir is meegegeven, één tab-gescheiden regel per bestand. Stopt met exitcode 2 als de map ontbreekt, geen map is of geen .sky-bestanden bevat.
---
Leest de doelmap uit `"$SKY_DIR"`.
