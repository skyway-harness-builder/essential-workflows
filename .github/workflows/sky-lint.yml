name: Lint workflows
on:
  push:
  pull_request:

jobs:
  sky-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: skylence-be/sky-lint-action@v2
        with:
          paths: "**/*.sky"
