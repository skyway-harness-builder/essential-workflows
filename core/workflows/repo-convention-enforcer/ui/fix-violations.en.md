---
short_description: Finds and fixes code style problems.
long_description: Runs the project's formatting and linting tools, fixes what it safely can, commits the result, and pushes it for review.
---
Checks the code for formatting and style problems and fixes as many as it safely can on its own. Anything that still needs a human decision is listed clearly. The changes are committed and pushed so they are ready for the next step: your review.
