---
short_description: Opens the pull request.
long_description: Pushes the final branch and opens a draft pull request linked to the source issue, filling in the repo's PR template when one exists.
---
Wraps up the approved implementation by opening a draft pull request. It links back to the original issue and fills in the project's pull request template if one is set up, so the PR is ready for a final human review.
