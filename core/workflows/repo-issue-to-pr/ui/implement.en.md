---
short_description: Writes the code for the issue.
long_description: Reads the issue description and implements the requested change, running the project's tests until they pass, then pushes a branch.
---
Reads the issue from top to bottom and writes the code to address it, running the project's tests along the way and fixing anything that breaks. Once the change is complete, it commits the work and pushes a branch so it is ready for review.
