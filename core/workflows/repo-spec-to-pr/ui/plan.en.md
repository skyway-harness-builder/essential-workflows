---
short_description: Writes the implementation plan.
long_description: Reads the spec and produces a structured plan covering which files change, the approach and risks, and the test strategy — without writing any code yet.
---
Reads through the spec and drafts a plan for how to implement it: which files need to change and why, what could go wrong, and how it will be tested. No code is written at this stage — it is plan only, so you can review the approach before any work begins.
